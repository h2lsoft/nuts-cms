/*html = $('#colText').html();
$('#colText').html('<iframe style="width:850px; height:400px;"></iframe>');

$('p.bottom').before( $frame );
setTimeout( function() {
	var doc = $frame[0].contentWindow.document;
	var $body = $('body', doc);
	$body.html('test');}, 1 );
*/